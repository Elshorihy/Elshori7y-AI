const chat=document.getElementById('chat');const prompt=document.getElementById('prompt');const send=document.getElementById('sendBtn');const fileInput=document.getElementById('fileInput');const uploadBtn=document.getElementById('uploadBtn');const attachBtn=document.getElementById('attachBtn');const themeBtn=document.getElementById('themeBtn');const menuBtn=document.getElementById('menuBtn');const sidebar=document.querySelector('.sidebar');const libraryStatus=document.getElementById('libraryStatus');
const DB_NAME='elshori7y_ai';const STORE='chunks';
function db(){return new Promise((resolve,reject)=>{const r=indexedDB.open(DB_NAME,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE,{keyPath:'id'});r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
async function saveChunks(chunks){const d=await db();return new Promise((resolve,reject)=>{const tx=d.transaction(STORE,'readwrite');const s=tx.objectStore(STORE);for(const c of chunks)s.put(c);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}
async function allChunks(){const d=await db();return new Promise((resolve,reject)=>{const tx=d.transaction(STORE,'readonly');const r=tx.objectStore(STORE).getAll();r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
function words(s){return [...new Set((s||'').toLowerCase().replace(/[^\p{L}\p{N}]+/gu,' ').split(/\s+/).filter(x=>x.length>2))]}
async function relevantContext(q){const chunks=await allChunks();if(!chunks.length)return '';const qwords=words(q);const scored=chunks.map(c=>{let score=0;const t=(c.text||'').toLowerCase();for(const w of qwords){if(t.includes(w))score+=w.length>4?2:1}return {...c,score}}).sort((a,b)=>b.score-a.score);const chosen=scored.filter(x=>x.score>0).slice(0,6);if(!chosen.length)return '';return chosen.map(x=>`[${x.book} — صفحة ${x.page}]\n${x.text}`).join('\n\n---\n\n').slice(0,18000)}
async function updateLibrary(){try{const c=await allChunks();const books=[...new Set(c.map(x=>x.book))];libraryStatus.textContent=books.length?`📚 ${books.length} كتاب محفوظ`:'لا توجد كتب مضافة'}catch{}}
function addMessage(text,type){const el=document.createElement('div');el.className='message '+type;el.textContent=text;chat.appendChild(el);chat.scrollTop=chat.scrollHeight;return el}
async function submit(text){text=(text||prompt.value).trim();if(!text)return;document.getElementById('welcome')?.remove();addMessage(text,'user');prompt.value='';send.disabled=true;const loading=addMessage('جاري البحث في كتبك والتفكير... 🤔','ai');try{const context=await relevantContext(text);const res=await fetch('/api/chat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message:text,context})});const data=await res.json();loading.remove();if(!res.ok)throw new Error(data.error||'حدث خطأ');addMessage(data.text||'لم يصل رد من المساعد.','ai')}catch(err){loading.remove();addMessage('❌ '+(err.message||'تعذر الاتصال بالمساعد.'),'ai')}finally{send.disabled=false;prompt.focus()}}
async function indexPdf(file){if(!window.pdfjsLib)throw new Error('قارئ PDF لم يتم تحميله. أعد تحميل الصفحة.');pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';const bytes=await file.arrayBuffer();const pdf=await pdfjsLib.getDocument({data:bytes}).promise;const chunks=[];for(let p=1;p<=pdf.numPages;p++){const page=await pdf.getPage(p);const content=await page.getTextContent();const text=content.items.map(i=>i.str).join(' ').replace(/\s+/g,' ').trim();if(!text)continue;for(let start=0;start<text.length;start+=1500){const part=text.slice(Math.max(0,start-180),start+1500);chunks.push({id:`${file.name}:${p}:${start}`,book:file.name,page:p,text:part})}}await saveChunks(chunks);return {pages:pdf.numPages,chunks:chunks.length}}
async function handleFile(file){if(!file)return;document.getElementById('welcome')?.remove();addMessage('📚 '+file.name,'user');const loading=addMessage('جاري قراءة الكتاب وفهرسته على جهازك... ⏳','ai');fileInput.value='';try{const result=await indexPdf(file);loading.textContent=`✅ تم تجهيز ${file.name}: ${result.pages} صفحة، وأصبح الكتاب متاحًا للمساعد. اسألني عنه الآن.`;await updateLibrary()}catch(e){loading.textContent='❌ تعذر قراءة الملف: '+(e.message||'خطأ غير معروف')}}
send.addEventListener('click',()=>submit());prompt.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit()}});document.querySelectorAll('[data-prompt]').forEach(b=>b.addEventListener('click',()=>submit(b.dataset.prompt)));uploadBtn.addEventListener('click',()=>fileInput.click());attachBtn.addEventListener('click',()=>fileInput.click());fileInput.addEventListener('change',()=>handleFile(fileInput.files[0]));themeBtn.addEventListener('click',()=>{document.body.classList.toggle('light');themeBtn.textContent=document.body.classList.contains('light')?'☀':'☾'});menuBtn.addEventListener('click',()=>sidebar.classList.toggle('open'));document.getElementById('newChat').addEventListener('click',()=>location.reload());updateLibrary();