(()=>{const prompt=document.getElementById('prompt'),composer=document.querySelector('.composer');if(!prompt||!composer)return;const SpeechRecognition=window.SpeechRecognition||window.webkitSpeechRecognition;const mic=document.createElement('button');mic.id='voiceBtn';mic.type='button';mic.title='تحدث مع Elshori7y AI';mic.textContent='🎤';composer.insertBefore(mic,document.getElementById('sendBtn'));let rec=null,listening=false;if(SpeechRecognition){rec=new SpeechRecognition();rec.lang='ar-EG';rec.interimResults=true;rec.continuous=false;rec.onstart=()=>{listening=true;mic.textContent='⏹️';mic.classList.add('active')};rec.onend=()=>{listening=false;mic.textContent='🎤';mic.classList.remove('active')};rec.onerror=()=>{listening=false;mic.textContent='🎤';mic.classList.remove('active')};rec.onresult=e=>{let finalText='';for(let i=e.resultIndex;i<e.results.length;i++)finalText+=e.results[i][0].transcript;if(finalText)prompt.value=finalText}}else{mic.disabled=true;mic.title='التحدث الصوتي غير مدعوم في هذا المتصفح'}mic.onclick=()=>{if(!rec)return;if(listening)rec.stop();else{rec.lang=/[\u0600-\u06ff]/.test(prompt.value)?'ar-EG':'ar-EG';rec.start()}};const speakBtn=document.createElement('button');speakBtn.id='speakBtn';speakBtn.type='button';speakBtn.title='قراءة آخر رد';speakBtn.textContent='🔊';composer.parentElement.insertBefore(speakBtn,composer);let lastAnswer='';const observer=new MutationObserver(()=>{const msgs=document.querySelectorAll('.message.ai');const last=msgs[msgs.length-1];if(last&&last.textContent&&!last.textContent.startsWith('جاري')&&!last.textContent.startsWith('❌'))lastAnswer=last.textContent});observer.observe(document.getElementById('chat'),{childList:true,subtree:true});speakBtn.onclick=()=>{if(!lastAnswer||!window.speechSynthesis)return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(lastAnswer);u.lang='ar-EG';u.rate=.95;u.pitch=1;speechSynthesis.speak(u)};window.elshori7yVoice={speak:text=>{if(!text)return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='ar-EG';u.rate=.95;speechSynthesis.speak(u)}}})();